--[[ HatchEgg RemoteFunction ]]
-- Client calls this to request hatching an egg. Returns monster data.

local remoteFunction = Instance.new("RemoteFunction")
remoteFunction.Name = "HatchEgg"
remoteFunction.Parent = script.Parent

return remoteFunction